use super::parsing;

pub struct Output;

pub fn analyze(literals: Vec<parsing::FieldArgument>, schema: ()) -> Output {
    // Somehow implement this.
    // Though it's not that simple, as I really need more info than the schema parsing in this module
    // currently gives me.

    todo!("analyze not implemented")
}
