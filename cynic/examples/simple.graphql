scalar JSON

type Query {
  testStruct: TestStruct
  myUnion: MyUnionType
}

type TestStruct {
  fieldOne(x: Int, y: String): String!
  nested: Nested!
  optNested: Nested
  json: JSON
}

union MyUnionType = Nested | TestStruct

type Nested {
  aString: String!
  optString: String
}
