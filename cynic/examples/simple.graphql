scalar JSON

type Query {
  testStruct: TestStruct
}

type TestStruct {
  fieldOne(x: Int, y: String): String!
  nested: Nested!
  optNested: Nested
  json: JSON
}

type Nested {
  aString: String!
  optString: String
}
